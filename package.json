{
  "name": "praceprojuniora",
  "private": true,
  "type": "module",
  "scripts": {
    "release": "release-it",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.6",
    "bun-types": "^1.3.10",
    "prettier": "^3.7.4",
    "turbo": "^2.8.17",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "release-it": "^20.0.0"
  }
}
